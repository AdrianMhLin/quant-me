var Quantme = Quantme || { Models: {}, Collections: {}, Views: {} };

Quantme.Models.CategoryModel = Backbone.Model.extend({
	initialize: function(){
		console.log("Boom! New categoryModel created!");
	}
});